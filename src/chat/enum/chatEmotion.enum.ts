/**
 * Emotion variants available for predefined clan-chat responses.
 * Values match the frontend ChatEmotion enum.
 */
export enum ChatEmotion {
  BLANK = -1,
  SORROW = 0,
  JOY = 1,
  PLAYFUL = 2,
  ANGER = 3,
  LOVE = 4,
}
