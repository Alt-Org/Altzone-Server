/**
 * Enum representing the names of different voting queues.
 *
 * Should be used when adding a job to the voting queue as the name of the job.
 */
export enum VotingQueueName {
  CLAN_SHOP = 'clan_shop',
  FLEA_MARKET = 'flea_market',
}
